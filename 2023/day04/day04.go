package day04

import (
	"adventofcode/helper"
	"strconv"
	"strings"
)

func Star1(input *hewlper.InputReader) (string, error) {
	var res int

	for line := range input.IterateLines {

	}

	return strconv.Itoa(res), nil
}

func parseLine(line string) (win, got []int) {
	for _, word := range strings.Fields(lin)
}

func Star2(input *helper.InputReader) (string, error) {
	var res int

	return strconv.Itoa(res), nil
}
